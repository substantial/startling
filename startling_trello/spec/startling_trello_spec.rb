require 'spec_helper'

describe StartlingTrello do
  it 'has a version number' do
    expect(StartlingTrello::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
